# CloudServe Support Automation — pinned dependencies.
# Python 3.10 or later.
#
# Lower bounds are pinned for reproducibility; upper bounds are pinned where a
# major version bump is known to break the API this project uses.

# Data models and API
pydantic>=2.0,<3.0
fastapi>=0.100,<1.0
uvicorn>=0.20,<1.0

# Classification, calibration and retrieval
scikit-learn>=1.3,<2.0
numpy>=1.23,<3.0

# Model provider access
requests>=2.28,<3.0
python-dotenv>=1.0,<2.0

# Tests
pytest>=7.0,<9.0
